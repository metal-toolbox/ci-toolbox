name: trivy scan

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      runs-on:
        type: string
        required: false
        default: ubuntu-latest
      scan-type:
        type: string
        required: false
        default: fs
        description: 'The type of scan: fs (filesystem), image, repo, etc.'
      image-ref:
        type: string
        required: false
        description: 'The image to scan (required if scan-type is image)'
      scan-ref:
        type: string
        required: false
        default: '.'
        description: 'The path or reference to scan (default: . for filesystem)'

jobs:
  trivy-scan:
    runs-on: ${{ inputs.runs-on }}
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: full scan with Trivy
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # 0.36.0
        env:
          TRIVY_DEBUG: true
        with:
          scan-type: ${{ inputs.scan-type }}
          image-ref: ${{ inputs.image-ref }}
          scan-ref: ${{ inputs.scan-ref }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          exit-code: '0'
      - name: fail with high critical
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # 0.36.0
        with:
          scan-type: ${{ inputs.scan-type }}
          image-ref: ${{ inputs.image-ref }}
          scan-ref: ${{ inputs.scan-ref }}
          format: 'table'
          severity: 'HIGH,CRITICAL'
          output: 'trivy-results.txt'
          exit-code: '1'
      - name: job summary
        run: |
          # step summary
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat trivy-results.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
      - name: Upload Trivy results to Security tab
        if: always()
        uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          sarif_file: 'trivy-results.sarif'
